import React from 'react';
import './filter.scss';
const Filter = () => {
    return (
        <>
            <button className="filter">Filter by name</button>
        </>
    );
};

export default Filter;
